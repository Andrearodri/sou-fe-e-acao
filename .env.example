# Optional client-side Supabase Auth configuration.
# Flutter does not load this file automatically. Pass these names through
# --dart-define when running or building the app; never commit real values.
SUPABASE_URL=
SUPABASE_ANON_KEY=
